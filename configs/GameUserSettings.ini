[/Script/Dispatch.DispatchLocalSettings]
AnalyticsPermission=Allowed
bUseDynamicResolution=False              // Keep dynamic resolution OFF (prevents stutter)

ResolutionSizeX=1024                     // Change from 1280 → 1024 (lower GPU load)
ResolutionSizeY=576                      // Change from 720 → 576 (16:9, no black bars)
LastUserConfirmedResolutionSizeX=1024    // Match new resolution
LastUserConfirmedResolutionSizeY=576     // Match new resolution

FullscreenMode=1                         // Change from mixed values → Borderless Fullscreen
LastConfirmedFullscreenMode=1            // Fix fullscreen mismatch issue
PreferredFullscreenMode=1                // Force borderless fullscreen

WindowPosX=-1
WindowPosY=-1

FrameRateLimit=30.000000                 // Change from 0 (unlimited) → 30 FPS (stable gameplay)

DesiredScreenWidth=400                   // Change from 1280 → 400
DesiredScreenHeight=240                  // Change from 720 → 240
LastUserConfirmedDesiredScreenWidth=1024 // Sync desired resolution
LastUserConfirmedDesiredScreenHeight=576 // Sync desired resolution

Version=5
AudioQualityLevel=0
LastConfirmedAudioQualityLevel=0

LastRecommendedScreenWidth=-1.000000
LastRecommendedScreenHeight=-1.000000
LastCPUBenchmarkResult=-1.000000
LastGPUBenchmarkResult=-1.000000
LastGPUBenchmarkMultiplier=1.000000

bUseHDRDisplayOutput=False               // Disable HDR (saves GPU resources)
HDRDisplayOutputNits=1000


[ScalabilityGroups]
sg.ResolutionQuality=60.000000            // Change from 100 → 60 (internal resolution scale)
sg.ViewDistanceQuality=0                  // Change from 3 → 0 (shorter draw distance)
sg.AntiAliasingQuality=0                  // Change from 3 → 0 (disable AA)
sg.ShadowQuality=0                        // Change from 3 → 0 (disable shadows)
sg.PostProcessQuality=0                   // Change from 3 → 0 (disable bloom, DOF, etc.)
sg.TextureQuality=0                       // Change from 3 → 0 (lower texture detail)
sg.EffectsQuality=0                       // Change from 3 → 0 (disable heavy effects)
sg.FoliageQuality=0                       // Change from 3 → 0 (remove foliage load)
sg.ShadingQuality=0                       // Change from 3 → 0 (simpler lighting)


[/Script/Engine.GameUserSettings]
bUseDesiredScreenHeight=False              // Let engine use fixed resolution above


[ShaderPipelineCache.CacheFile]
LastOpened=Dispatch
